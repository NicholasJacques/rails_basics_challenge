<h1><%= @student.name %></h1>


<h3>Addresses<h3>
<ul>
  <% @student.addresses.each do |address| %>
    <li><%= address.address %></li>
  <% end %>
</ul>

<h3>Courses</h3>
<ul>
  <% @student.courses.each do |course| %>
    <li><%= course.name %></li>
  <% end %>
</ul>